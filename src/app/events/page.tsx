import ContentArea from '@/components/ContentArea';
import TextSection from '@/components/TextSection';

const ContactPage = () => {
    return (
        <ContentArea>
            <TextSection heading='Our events'>
                There are no events currently.
            </TextSection>
        </ContentArea>
    );
};

export default ContactPage;
